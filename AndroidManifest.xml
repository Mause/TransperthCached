<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.lysdev.transperthcached"
      android:versionCode="1"
      android:versionName="1.0">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application android:label="@string/app_name" >
        <activity android:name=".activities.TrainActivity" />
        <activity android:name=".activities.train.TrainLineSelectActivity" />
        <activity android:name=".activities.train.TrainStationSelectActivity" />
        <activity android:name=".activities.train.TrainStationTimesActivity" />

        <activity android:name=".activities.StopTimetableActivity" />
        <activity android:name=".activities.FavouriteStopsActivity" />

        <activity android:name=".activities.MainActivity"
                  android:label="@string/app_name"
                  android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
